<%= link_to 'Schools', schools_path if policy(School).index? %>
<%= link_to 'SchoolAdmins', school_admins_path if policy(SchoolAdmin).index? %>
<%= link_to 'Courses', courses_path if policy(Course).index? %>
<%= link_to 'Batches', batches_path if policy(Batch).index? %>
<%= link_to 'Enrollments', enrollments_path if policy(Enrollment).index? %>

<%= button_to 'Sign Out', destroy_user_session_path, method: :delete %>
