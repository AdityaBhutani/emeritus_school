<p style="color: green"><%= notice %></p>

<h1>Batches</h1>

<div id="batches">
  <% @batches.each do |batch| %>
    <%= render batch %>
  <% end %>
</div>

<%= link_to "New batch", new_batch_path %>
